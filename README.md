# Pooja Gadhave - Portfolio (React + Vite + Tailwind)

## Setup
1. Install dependencies: `npm install`
2. Run locally: `npm run dev`
3. Build for production: `npm run build`

Place your resume in `public/resume.pdf`. Update project links in `src/components/Projects.jsx`. 
